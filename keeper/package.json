{
  "name": "cap-auto-pounder-keeper",
  "version": "1.0.0",
  "description": "Off-chain keeper for CAPAutoPounder — fetches EigenLayer merkle proofs and calls compound()",
  "type": "module",
  "scripts": {
    "compound": "npx tsx src/compound.ts",
    "check-rewards": "npx tsx src/check-rewards.ts"
  },
  "dependencies": {
    "viem": "^2.21.0"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "@types/node": "^22.0.0"
  }
}
